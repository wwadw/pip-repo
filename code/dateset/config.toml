# ROS图像数据采集配置文件

[collector]
# ROS图像话题名称
topic = "/camera/image_raw"

# 采集间隔帧数 (每隔多少帧保存一张图片)
interval = 10

# 图片保存目录
save_dir = "./images"

# 图片格式: jpg 或 png
format = "jpg"

# 图片文件名前缀
prefix = "img"

# 图片质量 (1-100, 仅对jpg有效)
quality = 95
